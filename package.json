{
  "name": "flakeradar-program",
  "version": "1.0.0",
  "description": "FlakeRadar program repository - Gate A deliverables",
  "main": "index.js",
  "scripts": {
    "test:schema": "ajv validate -s schemas/issues.seed.schema.json -d issues/seed.json --spec=draft2020 --strict=true --validate-formats=false",
    "validate": "ajv validate -s schemas/issues.seed.schema.json -d issues/seed.json --spec=draft2020 --strict=true --validate-formats=false"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1"
  },
  "keywords": ["flakeradar", "flaky-tests", "ci", "github-actions"],
  "author": "rick1330",
  "license": "Apache-2.0"
}